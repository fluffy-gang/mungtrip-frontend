import { styled } from 'styled-components/native';

import { Text } from '@/components/ui/text';

export const UnitText = styled(Text).attrs({
  color: 'textSecondary',
  fontSize: 16,
  fontWeight: 'bold',
  lineHeight: 24,
})``;
